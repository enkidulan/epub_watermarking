[buildout]
parts =
    django
    python
    services_config

extends =
    versions.cfg
versions = versions
find-links =
eggs =


[django]
recipe = djangorecipe
settings = development
eggs =
    ${buildout:eggs}
    django
    gunicorn
    lxml
    pyyaml
    arrow
extra-paths =
project = watermarking_web_app


[python]
recipe = zc.recipe.egg:script
dependent-scripts = true
eggs = ${django:eggs}
interpreter = python


[start_on_reboot]
recipe = z3c.recipe.usercrontab
times = @reboot
